import Cocoa
import FlutterMacOS

public class RestsendDartPlugin: NSObject, FlutterPlugin {
  public static func register(with registrar: FlutterPluginRegistrar) {
    // No-op, just to satisfy the Flutter plugin system
  }
}
